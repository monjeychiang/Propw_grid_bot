# PropW Grid Trading Bot

<div align="center">

![Version](https://img.shields.io/badge/version-2.3.0-blue.svg)
![Python](https://img.shields.io/badge/python-3.11+-brightgreen.svg)
![React](https://img.shields.io/badge/react-18.x-61dafb.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)

**自動化網格交易機器人，專為 PropW 交易所設計**

[功能特色](#-功能特色) • [快速開始](#-快速開始) • [使用指南](#-使用指南) • [開發文檔](#-開發文檔)

</div>

---

## 📋 項目概述

**PropW Grid Trading Bot** 是一套完整的自動化網格交易系統，透過瀏覽器自動化技術 (Playwright) 與 PropW 交易所互動，實現全自動的低買高賣網格策略。

### 🎯 開發背景

**PropW** 是一個加密貨幣交易考試平台 (Prop Trading Firm)，交易者需要通過平台設定的考核規則才能獲得正式交易資格。考核期間有嚴格的風控要求，包括：

- 📉 **最大虧損限制**: 不得超過初始資金的一定比例
- 📊 **每日虧損上限**: 單日回撤不得過大
- 💰 **獲利目標**: 需在期限內達成指定盈利目標
- ⏰ **交易天數要求**: 需有最低交易活躍天數

基於這些規則，我們選擇 **網格交易策略** 作為主要方法：

1. **穩定獲利**: 網格策略透過低買高賣，在震盪行情中持續累積小額利潤
2. **風險可控**: 設定明確的價格區間，避免追漲殺跌
3. **自動執行**: 24 小時不間斷運行，不錯過任何交易機會
4. **心態穩定**: 機械化執行，避免情緒化交易

> 💡 **通過考核後**，本系統可繼續使用於正式帳戶，持續以網格策略賺取穩定收益。

### ✨ 功能特色

| 功能 | 說明 |
|------|------|
| 🔐 **智能登入** | 一鍵開啟登入頁面，支援手動完成驗證碼後自動偵測 |
| 📊 **網格策略** | 可視化設定價格區間、網格數量與投資額 |
| ⚡ **零延遲價格** | 攔截交易所 WebSocket，獲取實時市場數據 |
| 🔄 **自動補單** | 買單成交 → 自動掛賣單；賣單成交 → 自動掛買單 |
| 📈 **即時進度** | 策略啟動時顯示訂單建立進度 (例如: 5/10) |
| 🎨 **現代介面** | 毛玻璃風格 UI，價格漲跌即時變色 |
| 📝 **時間戳記錄** | 後端日誌包含完整時間戳，便於除錯 |

---

## 🏗 系統架構

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              Frontend (React)                            │
│   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                  │
│   │ Layout       │  │ Strategies   │  │ Strategy     │                  │
│   │ (價格/狀態)   │  │ Page (列表)  │  │ Detail (詳情)│                  │
│   └──────┬───────┘  └──────┬───────┘  └──────────────┘                  │
│          │ WebSocket       │ REST API                                    │
└──────────┴─────────────────┴────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                              Backend (FastAPI)                           │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                     API Routes Layer                             │   │
│   │   /bot/open-login  /strategies  /orders  /ws                    │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                     Services Layer                               │   │
│   │   PropwBot │ GridExecutor │ PriceInterceptor │ FillChecker      │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│   ┌────────────────┐                                                    │
│   │  SQLite (DB)   │                                                    │
│   └────────────────┘                                                    │
└─────────────────────────────────────────────────────────────────────────┘
                    │
                    ▼ Playwright
┌─────────────────────────────────────────────────────────────────────────┐
│                          PropW Exchange                                  │
│   Browser Automation │ WebSocket Interception │ Order Execution         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 技術棧

- **Backend**: Python 3.11, FastAPI, Playwright, SQLAlchemy
- **Frontend**: React 18, TypeScript, Vite, Tailwind CSS v4
- **Database**: SQLite
- **Real-time**: WebSocket (雙向通訊)

---

## 🚀 快速開始

### 前置需求

- Python 3.11+
- Node.js 18+
- Google Chrome

### 安裝步驟

```powershell
# 1. Clone 專案
git clone https://github.com/your-username/propw-bot.git
cd propw-bot

# 2. 後端設定
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
playwright install chromium

# 3. 前端設定
cd frontend
npm install
cd ..

# 4. 啟動系統
run_app.bat
```

### 環境變數 (.env)

```env
SIMULATE_ORDERS=false     # true = 模擬模式 (不真實下單)
HEADLESS_BROWSER=false    # true = 無頭模式 (不顯示瀏覽器)
```

---

## 💡 使用指南

### 1. 登入交易所

1. 點擊右上角 **LOGIN** 按鈕
2. 系統會自動開啟 PropW 登入頁面
3. 在彈出的瀏覽器視窗中完成登入 (包括驗證碼)
4. 登入成功後，前端會顯示 **LOGGED IN** 狀態

> 💡 **智能跳轉**: 登入後若停留在其他頁面，系統會自動導回交易頁面

### 2. 建立網格策略

1. 點擊 **+ 建立新策略**
2. 設定參數:
   - **策略名稱**: 自訂名稱
   - **交易對**: BTC/USDT, ETH/USDT 等
   - **價格區間**: 下限 ~ 上限
   - **網格數量**: 建議 5-20 格
   - **每格投資額**: 單格 USDT 金額
3. 確認建立

### 3. 啟動策略

- 點擊策略卡片上的 **啟動** 按鈕
- 按鈕會顯示即時進度: `啟動中 (3/10)`
- 所有訂單掛出後，策略狀態變為 **運行中**

### 4. 監控與停止

- **詳細資訊**: 查看網格層級、訂單紀錄
- **停止策略**: 點擊停止按鈕 (注意: 需手動撤銷交易所掛單)

---

## 📁 專案結構

```
Propw/
├── backend/
│   ├── routes/
│   │   ├── bot.py              # 機器人控制 API
│   │   ├── strategies.py       # 策略 CRUD
│   │   └── orders.py           # 訂單管理
│   ├── services/
│   │   ├── propw_bot.py        # Playwright 自動化
│   │   ├── grid_strategy_executor.py  # 網格執行器
│   │   ├── price_interceptor.py       # 價格攔截
│   │   └── fill_checker.py     # 成交檢測
│   └── main.py                 # FastAPI 入口
├── frontend/
│   ├── src/
│   │   ├── components/Layout.tsx   # 主框架 + WebSocket
│   │   └── pages/
│   │       ├── StrategiesPage.tsx  # 策略列表
│   │       └── StrategyDetailPage.tsx  # 策略詳情
│   └── package.json
├── run_app.bat                 # 一鍵啟動腳本
├── README.md                   # 開發文檔 (本文件)
└── USER_GUIDE.md              # 使用者手冊
```

---

## 🔧 開發文檔

### API 端點一覽

| 端點 | 方法 | 說明 |
|------|------|------|
| `/api/bot/start` | POST | 啟動瀏覽器 |
| `/api/bot/open-login` | POST | 開啟登入頁面 (新) |
| `/api/bot/status` | GET | 查詢狀態 (含自動跳轉) |
| `/api/strategies` | GET/POST | 策略 CRUD |
| `/api/strategies/{id}/start` | POST | 啟動策略 |
| `/api/strategies/{id}/stop` | POST | 停止策略 |
| `/ws` | WebSocket | 實時價格與事件推送 |

### WebSocket 事件

```typescript
// 價格更新
{ "type": "price_update", "data": { "price": "95000.5" } }

// 訂單建立
{ "type": "order_created", "data": { "strategy_id": 1, "side": "BUY", "price": 94000 } }

// 策略啟動完成
{ "type": "strategy_started", "data": { "strategy_id": 1 } }

// 訂單成交
{ "type": "order_filled", "data": { "order_id": 123, "price": "94000" } }
```

### 自動化邏輯 (PropwBot)

1. **登入流程**: 開啟登入頁面 → 等待使用者完成驗證 → 偵測登入狀態 → 自動跳回交易頁
2. **下單流程**: 選擇訂單類型 → 填入價格/數量 → 點擊下單按鈕
3. **價格攔截**: 監聽 `wss://ws.futurescw.com/market` → 解析 JSON → 廣播給前端

---

## 🐛 故障排除

| 問題 | 解決方案 |
|------|----------|
| 瀏覽器沒有啟動 | 確認已執行 `playwright install chromium` |
| 404 錯誤 (API) | 重啟後端伺服器 |
| 價格顯示 `--` | 確認已登入且在交易頁面 |
| 登入後沒有跳轉 | 等待 3 秒後系統會自動偵測並跳轉 |

---

## 📝 版本歷史

### v2.3.0 (2025-12-06)
- ✨ **優化登入流程**: 移除前端輸入框，直接開啟登入頁面
- ✨ **自動跳轉**: 登入後自動返回交易頁面
- ✨ **動態輪詢**: 未登入時 3 秒、已登入後 15 秒
- ✨ **價格趨勢**: 綠色上漲 / 紅色下跌
- ✨ **啟動進度**: 顯示訂單建立進度 (X/Y)
- 🔧 **日誌優化**: 增加時間戳

### v2.2.0 (2025-12-04)
- ✨ 實時價格數據 (WebSocket 攔截)
- ✨ 訂單成交監控
- ✨ 網格策略完整實作

### v2.0.0 (2025-12-03)
- 🎨 全新 Glassmorphism UI
- ✨ 策略管理系統
- ✨ 訂單追蹤

---

## 📄 授權條款

MIT License - 詳見 [LICENSE](LICENSE)

> ⚠️ **免責聲明**: 本專案僅供學習與研究用途，使用者需自行承擔交易風險。

---

## 🎯 未來規劃

- [ ] 支援多帳號管理
- [ ] 止損/止盈功能
- [ ] Telegram 通知
- [ ] Docker 部署
- [ ] 支援其他交易所

---

<div align="center">

**Made with ❤️ for algorithmic trading enthusiasts**

</div>
